import type { NormRect } from '../vision/frame';
import type { HsvRange } from '../vision/hsv';

/**
 * 官方操作手客户端 HUD 的版面常量 —— 全部由 1920×1080 赛事录屏逆向量出，再归一化。
 * 归一化是必须的：站点会按窗口大小取 1080p/720p/540p 三档源，绝对坐标一换档就废。
 *
 * DJI 改版会先打到这个文件。改版时只需重新量这里的矩形与阈值，算法本身不用动。
 */

/** 地面机器人的小地图。自机以绿色圆盘 + 外挂三角呈现，圆心=位置，三角方位=朝向。 */
export const MINIMAP: NormRect = { x0: 1460 / 1920, y0: 773 / 1080, x1: 1860 / 1920, y1: 998 / 1080 };

/**
 * 空中机器人的小地图。位置不同、也更小 —— 无人机那一路的 FPV 是内嵌在左侧子画面里的
 * （右侧是飞手与云台手两路操作手摄像头），所以整套 HUD 跟着一起缩了。
 *
 * 自机图标是四旋翼形状（480p 下糊成圆角矩形），同样带一个绿色朝向箭头。
 *
 * 实测两路渲染并不对称：蓝方是整个机身图标为绿色（面积中位 22px，91% 的帧可检出），
 * 红方只有朝向箭头是绿的、机身是白/灰（面积中位 3px，仅 3% 可检出）—— 因为红方图标
 * 叠了一层红色阵营底色，机身被压成发白。红方那 4px 的绿点质心散布整个 ROI，与地图上
 * 其他绿色元素无法区分，480p 下不可用。
 * 生产的 1080p 这块 ROI 面积是 4.5 倍，那 3px 碎片会变成约 14px，
 * 值得开播后重新实测再决定要不要为无人机单开一套判据。
 */
export const DRONE_MINIMAP: NormRect = { x0: 975 / 1920, y0: 595 / 1080, x1: 1243 / 1920, y1: 735 / 1080 };

/** 左下血条整条。用于判 HUD 是否亮着，以及量填充长度。 */
export const HP_BAR: NormRect = { x0: 170 / 1920, y0: 918 / 1080, x1: 516 / 1920, y1: 956 / 1080 };

/**
 * 血量数字「xxx / xxx」。
 *
 * 这个窗不要外扩。实测（16 帧夹具，逐行统计）字形恒定落在血条内的固定带上，
 * 换算成 1080p 是 y∈[927,942]、x∈[221,311]，本矩形四周各留 3~6px 余量刚好。
 * 往上放 6px 就会把血条顶沿的白色高光框进来 —— 那道高光在自适应阈值下会连成一片，
 * 把相邻数字粘成一坨（实测 14 帧里有 6 帧因此切分失败）。
 * 往右放则会收进血条右端的斜切装饰。
 */
export const HP_TEXT: NormRect = { x0: 215 / 1920, y0: 923 / 1080, x1: 318 / 1920, y1: 949 / 1080 };

/**
 * 基地与前哨站血量。四个数字都长在顶部记分板上，全场十路画面共享同一份 ——
 * 任意一路读出来即可，多路之间可互相校验。
 *
 * 行带是逐帧掩码量出来的（852×480 下）：基地数字在 y26~35、前哨站在 y30~35。
 * 上下都紧挨着必须避开的东西：血条的上下沿横线（红 y23、蓝 y23~25 与 y36），
 * 以及「前哨站」那三个白色标签字（y19~25）。窗口再往外放一点就会把它们收进来。
 *
 * 注意每个回合都会重置 —— 实测蓝方前哨站 Round1 末 600、Round2 中 945，
 * 单调递减的假设只在回合内成立。
 */
export const RED_OUTPOST_HP: NormRect = { x0: 194 / 1920, y0: 65 / 1080, x1: 266 / 1920, y1: 83 / 1080 };
export const RED_BASE_HP: NormRect = { x0: 334 / 1920, y0: 58 / 1080, x1: 399 / 1920, y1: 81 / 1080 };
/**
 * 两条基地血量的窗都比数字本身紧：左右各有必须避开的装饰 ——
 * 红方左侧是黄色尖角（列点亮率 0.21~0.37），蓝方右侧是一道斜杠（0.36~0.57）。
 * 它们都是满高的，靠笔画高度过滤挡不住，会凑成第五个字形段让整帧读数作废。
 * 蓝方的上边界还比红方低 3px：蓝色血条顶沿高光更厚，会在数字顶部连成横条把中间几位粘住。
 */
export const BLUE_BASE_HP: NormRect = { x0: 1521 / 1920, y0: 61 / 1080, x1: 1584 / 1920, y1: 79 / 1080 };
export const BLUE_OUTPOST_HP: NormRect = { x0: 1645 / 1920, y0: 65 / 1080, x1: 1724 / 1920, y1: 83 / 1080 };

/**
 * 顶部记分板红方半区。只做一件事：判该机器人是否阵亡。
 * 阵亡时整个赛事 UI 灰度化，这里精确掉到 0；濒死（20/200）时仍有 0.57~0.72。
 * 注意它在等待卡/回合间歇也有色（0.55~0.61），所以只有在「回合进行中」成立后才可解读。
 */
export const TOP_SCOREBOARD: NormRect = { x0: 0.16, y0: 0.045, x1: 0.39, y1: 0.085 };

/**
 * 自机标记的绿。刻意避开能量机关的黄绿（色相≈42）和蓝方图标的青（色相≥90）。
 *
 * 放宽过一轮并被数据否决了 —— 在 1340 个存活帧上对比（每 6s 采样，7 路）：
 *     68-86 / S90 / V90   检出 93.7%   多候选 0.1%    ← 采用
 *     64-88 / S78 / V78   检出 94.3%   多候选 0.9%
 *     62-88 / S70 / V70   检出 94.6%   多候选 1.5%
 *     55-92 / S60 / V60   检出 95.2%   多候选 15.7%
 * 多检出不到 1pp，歧义却涨十几倍。漏检只让沙盘沿用上一次位置（下一帧通常就补回来），
 * 误检却会把机器人画到错误的格子上 —— 后者是观众看得见的错，前者不是。
 *
 * 已知边界：血量很低时客户端会叠红色伤害泛光，把标记的 S/V 一起压下去，
 * 极端情况下色相还会被拉到 66 左右而落到区间外（夹具 B3Infantry@1660 就是这种）。
 *
 * **上沿 86 → 90（2026-08-04）**。原值在浏览器上会切掉真实的标记像素。
 *
 * 起因是同一份 YUV 在两条链路上解出了不同的 RGB：现网 .ts 流不带色彩元数据
 * （`yuv420p` 裸的，无 VUI），解码器只能猜 —— ffmpeg 猜 BT.601、Chrome 猜 BT.709。
 * 同一个文件强制两种矩阵解码，自机绿的色相中位分别是 78.3 与 80.8，差 2.5。
 * 生产跑在浏览器里，所以 709 才是真相。
 *
 * 小地图色相直方图（S/V 已过门限，绿簇 → 谷底 → 蓝方青簇）：
 *     浏览器现网 1152×648    绿峰 79-81   谷底 87-91   青簇 93 起涨   ← 生产，709
 *     committed 夹具 852×480 绿峰 77-79   谷底 81-85   青簇 87 起涨   ← 601（见下）
 *     ffmpeg 抽的 .ts        绿峰 75-77   谷底 83-89   青簇 91 起涨   ← 601
 * 86 正落在 committed 那组的谷底 —— 对当初的标定集是准的，对浏览器则偏低了。
 * 浏览器那组的上尾还格外肥（83-87 区间 1791px，committed 只有 31px），
 * 4:2:0 色度上采样在绿/青交界处造出的过渡像素，分辨率越高这条尾巴越长。
 *
 * 上沿扫描（浏览器 138 个 HUD 亮着的地面帧 / ffmpeg 461 / committed 31）：
 *     hMax   浏览器检出   浏览器多候选   ffmpeg多候选   committed多候选
 *      86      72.5%          0%            0.4%            0%      ← 原值
 *      90      81.2%          0%            0.4%            0%      ← 采用
 *      92      85.5%          0%            0.4%            3.2%
 *      95      87.7%        1.4%            1.7%            9.7%
 *     100      94.2%       55.8%           75.7%           32.3%    ← 收进蓝方图标
 * 判据仍是多候选率（与上面那轮标定同一个）：90 是三组的多候选率**全都没有上升**
 * 的最远点，再往外就开始蚕食青簇。位移中位同步从 9.8px 降到 7.2px，说明放宽
 * 收回来的是真目标而不是杂色。
 *
 * **两条离线管线都是 601，只有生产是 709。** 起初以为 committed 夹具没这个问题
 * （源 mp4 明确标了 bt709），实测否决：同一个 mp4 三种解法比绿簇下沿 ——
 *     cv2（夹具管线）      p10 = 77.7
 *     ffmpeg 按标签(709)   p10 = 80.3
 *     ffmpeg 强制当 601    p10 = 77.6
 * cv2 与「强制 601」重合，即 **OpenCV 的 FFmpeg 后端不理会色彩标签**。所以
 * dump_fixtures.py 抽出来的也是 601 像素，标定基准整条链路都偏在同一侧。
 *
 * 连带结论：抽帧做标定必须显式指定色彩矩阵 —— ffmpeg 管线加
 * `scale=in_color_matrix=bt709`，cv2 那条则要换掉解码器（它没有对应开关）。
 * 在此之前，离线量到的绝对检出率不能直接外推到浏览器；跨管线只比**同一数据集上
 * 改一个参数带来的差分**，那是可信的。本条上沿修正就是这么定的。
 *
 * 同一个偏差原则上影响 layout 里所有色彩门限，只是别的窗要么在色相上宽得多、
 * 要么主要靠 S/V，没被咬到：浏览器实测血量读出 97.8%、目标读出 76%，都不低于离线。
 */
export const SELF_MARKER_GREEN: HsvRange = { hMin: 68, hMax: 90, sMin: 90, vMin: 90 };

/**
 * 空中机器人的最小面积，单独一条 —— 不能沿用地面那个比例。
 *
 * 红蓝两方的自机图标不是同一种颜色。实测（现网 1920×1080，图标 29×29 邻域）：
 *     蓝方  彩色亮像素 224px，色相分位(10/50/90) = [82,84,90]  紧聚在绿
 *     红方             116px                    = [11,78,177] 散在红↔绿之间
 * 红方图标叠了一层红色阵营底色，机身被压成发白只剩箭头还绿，落进 SELF_MARKER_GREEN
 * 的仅 26px；蓝方是 141px。而地面那个比例换算到无人机 ROI 是 45px —— 红方全被滤掉。
 *
 * 逐档扫描（400 帧 FPV，不含等待卡）：
 *     ratio    门限   红方检出   红方连续帧位移中位   蓝方检出
 *     0.0012   45px     1.2%          53.0px          90.2%   ← 那 1.2% 本身就是噪声
 *     0.0006   23px    24.5%           2.8px          92.5%
 *     0.0004   15px    78.0%           1.8px          93.2%
 *     0.0003   11px    89.0%           1.9px          93.5%   ← 采用
 * 判据是位移而不是检出率：门限 45px 时位移中位 53px，抓的是满 ROI 乱跳的杂色；
 * 降到 11px 后位移中位 1.9px，说明锁住了同一个目标。蓝方全程几乎不动，
 * 说明放低门限没有给它引入误检。
 *
 * 敢比地面放这么低，是因为无人机小地图上的绿色干扰本来就少 —— 地面那张图上还有
 * 能量机关的黄绿要防。
 */
export const DRONE_MARKER_MIN_AREA_RATIO = 0.0003;

/** 阵营色。红色跨 0 点，靠 HsvRange 的环形区间表达。 */
export const TEAM_BLUE: HsvRange = { hMin: 95, hMax: 115, sMin: 110, vMin: 70 };
export const TEAM_RED: HsvRange = { hMin: 172, hMax: 8, sMin: 110, vMin: 70 };

/** 血量数字：高亮、低饱和的白字。血条填满时底色是高饱和蓝/红，掉空时底色发暗，两种情况都能靠这条分出来。 */
export const HP_GLYPH: HsvRange = { hMin: 0, hMax: 179, sMin: 0, vMin: 140 };
/** 字形还要求"不饱和"，HsvRange 只能表达下限，所以饱和度上限单独放这里。 */
export const HP_GLYPH_MAX_SAT = 90;

/**
 * 自机标记的最小面积，按 ROI 面积的比例给 —— 面积随分辨率平方变化，
 * 写成绝对像素数的话 540p 会全部漏检、1080p 会放进噪声。
 * 480p 下小地图 ROI 约 17700px，本比例对应 ≈21px，实测圆盘稳定在 37~45px。
 */
export const SELF_MARKER_MIN_AREA_RATIO = 0.0012;

/**
 * 判「赛事 UI 已灰化 = 该机器人阵亡」的阵营色占比上限。
 * 实测（16 帧夹具）：存活 0.347~0.749，阵亡 0.000。
 */
export const DEAD_SCOREBOARD_SAT = 0.15;

/**
 * 判「记分板区域确实画了东西」的亮像素占比下限。
 *
 * 少了这一条就会把黑屏、丢帧、流断当成阵亡 —— 两者的饱和度同样是 0。
 * 区别在于阵亡时 UI 仍然被绘制、只是去了色：实测阵亡帧亮像素占 0.770，
 * 存活 0.79~0.98，而全黑帧是 0。
 */
export const DEAD_SCOREBOARD_MIN_LIT = 0.3;

/** 判「HUD 亮着」的血条阵营色下限。实测非比赛时段 ≤0.001，比赛中最低 0.147。 */
export const HUD_PRESENT_BAR_SAT = 0.03;

/**
 * 判「HUD 压根不在」的亮像素占比**上限**。
 *
 * 这一条与上面那条方向相反，判的也是另一回事：上面问「这一路开赛了吗」，
 * 这一条问「记分板区域里现在是 HUD 还是一张照片」。
 *
 * 赛事 HUD 无论是否灰化，元素之间永远留有深色底；导播切过去的转播镜头是一整张
 * 照片，几乎每个像素都过 V>60。现网 336 帧地面样本：
 *   - 能检出自机的 112 帧 lit ∈ [0.752, **0.948**]，一帧都没到 1.000
 *   - lit ≥ 0.999 的 120 帧里，检出自机 0 帧、读出血量 1 帧
 * 取 0.97：比 HUD 实测上界高 0.022，比转播镜头的 1.000 低 0.03，两侧都有余量。
 *
 * 这是「每一路自行判断是否开播」的核心判据 —— 赛中某台机器人缺席时导播只切那一路，
 * 全局标志对它是错的。
 */
export const HUD_ABSENT_LIT = 0.97;
